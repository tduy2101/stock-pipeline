# Warehouse

Thu muc nay chua schema va artifact lien quan PostgreSQL/TimescaleDB.

Trong Medallion MVP:

- Bronze khong vao PostgreSQL.
- PostgreSQL chi co schema `silver` va `gold`.
- DDL chinh nam tai `warehouse/ddl/schema.sql`.
